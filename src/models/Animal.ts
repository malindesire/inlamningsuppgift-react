export class Animal {
    constructor(public id: number, public name: string, public born: number, public about: string, public facts: string, public image: string, public isFed: boolean, public lastFed: Date) {}
}